endoscope_sfm:
  ros__parameters:
    # Ottimizzato per oggetti piccoli (20cm)
    min_baseline_meters: 0.003    # 3mm - movimento minimo per oggetto piccolo
    min_rotation_degrees: 0.8     # Rotazione minima per trigger
    ransac_threshold: 12.0        # Soglia RANSAC per fisheye